* {
    -webkit-tap-highlight-color: rgba(0,0,0,0); /* make transparent link selection, adjust last value opacity 0 to 1.0 */
}

body {
    -webkit-touch-callout: none;                /* prevent callout to copy image, etc when tap to hold */
    -webkit-text-size-adjust: none;             /* prevent webkit from resizing text to fit */
    -webkit-user-select: none;                  /* prevent copy paste, to allow, change 'none' to 'text' */
    background-color:#E4E4E4;
    background-image:linear-gradient(top, #A7A7A7 0%, #E4E4E4 51%);
    background-image:-webkit-linear-gradient(top, #A7A7A7 0%, #E4E4E4 51%);
    background-image:-ms-linear-gradient(top, #A7A7A7 0%, #E4E4E4 51%);
    background-image:-webkit-gradient(
        linear,
        left top,
        left bottom,
        color-stop(0, black),
        color-stop(0.90, black)
    );
    background-attachment:fixed;
    font-family:'HelveticaNeue-Light', 'HelveticaNeue', Helvetica, Arial, sans-serif;
    font-size:12px;
    height:100%;
    margin:0px;
    padding:0px;
    text-transform:uppercase;
    width:100%;
}

/* Portrait layout (default) */
.app {
    background:url(../img/logo.png) no-repeat center top; /* 170px x 200px */
    position:absolute;             /* position in the center of the screen */
    left:50%;
    top:50%;
    height:300px;                   /* text area height */
    width:340px;                   /* text area width */
    text-align:center;
    padding:180px 0px 75px 0px;     
    margin:-220px 0px 0px -170px;  
                                   
}

/* Landscape layout (with min-width) */
@media screen and (min-aspect-ratio: 1/1) and (min-width:400px) {
    .app {
        background-position:left center;
        padding:75px 0px 75px 170px;  /* padding-top + padding-bottom + text area = image height */
        margin:-200px 0px 0px -200px;  /* offset vertical: half of image height */
         height:290px;                   /* text area height */
    width:340px; 
    }
}

h1 {
    font-size:24px;
    font-weight:normal;
    margin:0px;
    overflow:visible;
    padding:0px;
    text-align:center;
}

.event {
    border-radius:4px;
    -webkit-border-radius:4px;
    color:#FFFFFF;
    font-size:12px;
    margin:0px 30px;
    padding:2px 0px;
}

.event.listening {
    background-color:#333333;
    display:block;
}

.event.received {
    background-color:#4B946A;
    display:none;
}

@keyframes fade {
    from { opacity: 1.0; }
    50% { opacity: 0.4; }
    to { opacity: 1.0; }
}
 
@-webkit-keyframes fade {
    from { opacity: 1.0; }
    50% { opacity: 0.4; }
    to { opacity: 1.0; }
}
 
.blink {
    animation:fade 3000ms infinite;
    -webkit-animation:fade 3000ms infinite;
}
.boton{
color: white;
	background-color:#2591B9;	
	border-color:#0012E6;
	border-radius: 8px;
	height: 50px;
	left: 200px;
	font-size: 30px;
   text-align: center;
   border-style: double;
   margin:0px 0px 0px 10px;
   text-decoration: none; 
	
}
@media screen and (min-aspect-ratio: 1/1) and (min-width:400px) {
    .boton {
        background-position:left center;
        padding:-10px 0px 75px 170px;  /* padding-top + padding-bottom + text area = image height */
        margin:-90px 0px 0px -180px;  /* offset vertical: half of image height */        
    }
}

#inicio
{
	color: white;
	border-radius: 15px;
	height: 300px;
	font-size: 30px;
   border-style: double;
   margin: -110px 0px 0px -30px;
}

@media screen and (min-aspect-ratio: 1/1) and (min-width:200px) {
    #inicio {
        background-position:left center;
        padding:5px 0px 0px 170px;  /* padding-top + padding-bottom + text area = image height */
        margin:-40px 10px 20px -210px;  /* offset vertical: half of image height */
                                      /* offset horizontal: half of image width and text area width */
    }
}

label
{
	text-align: center;
	margin: auto;	
}

@media screen and (min-aspect-ratio: 1/1) and (min-width:200px) {
    label {
        background-position:left center;
        padding:5px 0px 0px 170px;  /* padding-top + padding-bottom + text area = image height */
        margin:-40px 10px 20px -290px;  /* offset vertical: half of image height */
                                      /* offset horizontal: half of image width and text area width */
    }
}